#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <unistd.h>

int _putchar(char c);
void _puts(char *str);
int _strlen(char *s);
int handle_string(char *str);

#endif
